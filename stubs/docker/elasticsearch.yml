# Docker Compose configuration for Elasticsearch
# Generated by PhpHive CLI
#
# Services:
# - elasticsearch: Elasticsearch 8.x search and analytics engine
# - kibana: Kibana web interface for Elasticsearch (optional)
#
# Usage:
#   docker compose up -d    # Start services
#   docker compose down     # Stop services
#   docker compose logs     # View logs
#
# Access:
#   Elasticsearch: localhost:9200
#   Kibana: http://localhost:5601

version: "3.8"

services:
  # Elasticsearch Service
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: "{{CONTAINER_PREFIX}}-elasticsearch"
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - ELASTIC_PASSWORD={{ELASTICSEARCH_PASSWORD}}
      - xpack.security.enabled=true
      - xpack.security.http.ssl.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "{{ELASTICSEARCH_PORT}}:9200"
    volumes:
      - "{{VOLUME_PREFIX}}-elasticsearch-data:/usr/share/elasticsearch/data"
    networks:
      - "{{NETWORK_NAME}}"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -s -u elastic:{{ELASTICSEARCH_PASSWORD}} http://localhost:9200/_cluster/health || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 5

  # Kibana Service (Optional)
  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.0
    container_name: "{{CONTAINER_PREFIX}}-kibana"
    restart: unless-stopped
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
      - ELASTICSEARCH_USERNAME=elastic
      - ELASTICSEARCH_PASSWORD={{ELASTICSEARCH_PASSWORD}}
    ports:
      - "{{KIBANA_PORT}}:5601"
    networks:
      - "{{NETWORK_NAME}}"
    depends_on:
      - elasticsearch
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -s -I http://localhost:5601 | grep -q 'HTTP/1.1 302 Found'",
        ]
      interval: 30s
      timeout: 10s
      retries: 5

# Named volumes for data persistence
volumes:
  "{{VOLUME_PREFIX}}-elasticsearch-data":
    driver: local

# Network for service communication
networks:
  "{{NETWORK_NAME}}":
    driver: bridge
